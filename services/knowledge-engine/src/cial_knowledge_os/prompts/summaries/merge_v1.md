Create the final grounded {summary_type} summary from the APPROVED SOURCE SUMMARIES below.
Mode is {multi_document_mode}. For together, synthesize common material without claiming a comparison. For separate, retain a clearly named section for each source and then a short overall overview. For compare, explicitly cover common findings, differences, contradictions, unique information, and missing coverage.
Use only supplied text. Preserve citation markers exactly and put citations after supported claims. Remove any claim whose citation is absent. State evidence gaps instead of using outside knowledge. For action_items, include only actions explicitly supported by a cited source and never invent owners, dates, frequencies, priorities, dependencies, approvals, systems, or metrics.

TARGET LENGTH: {summary_length}
CUSTOM INSTRUCTIONS: {custom_instructions}
APPROVED SOURCE SUMMARIES:
{source_summaries}
