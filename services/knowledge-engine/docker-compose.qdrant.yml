services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: cial-knowledge-os-v1-dev-qdrant

    ports:
      - "6335:6333"

    volumes:
      - cial_qdrant_storage:/qdrant/storage

    restart: unless-stopped

volumes:
  cial_qdrant_storage:
