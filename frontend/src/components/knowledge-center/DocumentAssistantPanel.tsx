import { useMemo, useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import { Bookmark, FileText, Info, MessageSquareText, Sparkles, StickyNote } from 'lucide-react';
import { listMyNotes } from '@/api/client';
import type { CorpusDocument, DocumentPreview } from '@/api/types';
import AIComposer from '@/components/assistant/AIComposer';
import { cn } from '@/lib/utils';

type Tab='ai'|'notes'|'bookmarks'|'metadata'|'related';
export default function DocumentAssistantPanel({document,preview,onAsk,onCitation}:{document:CorpusDocument;preview:DocumentPreview|null;onAsk:(question:string)=>Promise<void>;onCitation:(page:number,chunkId?:string|null)=>void}){
  const[tab,setTab]=useState<Tab>('ai');const[question,setQuestion]=useState('');const[asking,setAsking]=useState(false);const notes=useQuery({queryKey:['document-linked-notes',document.id],queryFn:()=>listMyNotes({}),enabled:tab==='notes',retry:false});const linked=useMemo(()=>notes.data?.items.filter((note)=>note.linked_documents.some((item)=>item.id===document.id))??[],[document.id,notes.data]);
  const tabs:[Tab,string,typeof Sparkles][]=[['ai','AI',Sparkles],['notes','Notes',StickyNote],['bookmarks','Bookmarks',Bookmark],['metadata','Metadata',Info],['related','Related',FileText]];
  const submit=async()=>{if(!question.trim()||asking)return;setAsking(true);try{await onAsk(question.trim());}finally{setAsking(false);}};
  return <div className="flex h-full min-h-0 flex-col bg-white"><header className="flex h-12 items-center gap-2 border-b border-slate-200 px-4"><Sparkles size={16} className="text-primary"/><strong className="text-sm">AI Assistant</strong></header><div className="flex overflow-x-auto border-b border-slate-200 px-2">{tabs.map(([value,label,Icon])=><button key={value} onClick={()=>setTab(value)} className={cn('flex h-10 items-center gap-1.5 border-b-2 px-2 text-xs',tab===value?'border-primary font-semibold text-primary':'border-transparent text-slate-600')}><Icon size={13}/>{label}</button>)}</div><div className="min-h-0 flex-1 overflow-y-auto p-3">{tab==='ai'?<div className="space-y-3"><section className="rounded-xl border border-slate-200 p-3"><h3 className="text-sm font-semibold">Summary</h3><p className="mt-2 text-xs leading-5 text-slate-500">Document analysis is not available until a grounded analysis endpoint returns this content.</p></section><section className="rounded-xl border border-slate-200 p-3"><h3 className="text-sm font-semibold">Key Findings & Action Items</h3><p className="mt-2 text-xs leading-5 text-slate-500">Ask the document copilot to generate a cited response. No findings are fabricated here.</p></section><section className="rounded-xl border border-slate-200 p-3"><div className="flex items-center justify-between"><h3 className="text-sm font-semibold">Citations</h3><span className="text-xs text-slate-500">{preview?.chunk_id?1:0}</span></div>{preview?.chunk_id?<button onClick={()=>onCitation(preview.page??1,preview.chunk_id)} className="mt-2 w-full rounded-lg bg-slate-50 p-2 text-left text-xs"><span className="font-medium">Active passage</span><span className="ml-2 text-slate-500">Page {preview.page??1}</span><p className="mt-1 line-clamp-3 text-slate-600">{preview.highlight_text||preview.preview_text||'Passage synchronized with the viewer.'}</p></button>:<p className="mt-2 text-xs text-slate-500">Open a cited passage to synchronize evidence here.</p>}</section><section className="rounded-xl border border-slate-200 p-3"><h3 className="text-sm font-semibold">Suggested Questions</h3><div className="mt-2 flex flex-wrap gap-2">{['Summarize this document','Explain the current section','What recommendations are supported?'].map((value)=><button key={value} onClick={()=>setQuestion(value)} className="rounded-lg border border-slate-200 px-2 py-1.5 text-left text-xs hover:bg-slate-50">{value}</button>)}</div></section></div>:tab==='notes'?notes.isLoading?<p className="text-sm text-slate-500">Loading linked notes…</p>:linked.length?<div className="space-y-2">{linked.map((note)=><a key={note.id} href={`/workspace/notes?note=${note.id}`} className="block rounded-lg border border-slate-200 p-3"><strong className="text-sm">{note.title}</strong><p className="mt-1 line-clamp-2 text-xs text-slate-500">{note.plain_text||'Empty note'}</p></a>)}</div>:<p className="rounded-lg border border-dashed border-slate-200 p-4 text-sm text-slate-500">No notes are linked to this document.</p>:tab==='bookmarks'?<p className="rounded-lg border border-dashed border-slate-200 p-4 text-sm text-slate-500">Persisted document bookmarks are unavailable because no bookmark API is configured.</p>:tab==='metadata'?<dl className="space-y-3 text-sm">{[['Type',document.file_type.toUpperCase()],['Size',`${Math.round(document.size_bytes/1024)} KB`],['Pages',document.page_count??preview?.page_count??'Unknown'],['Modified',document.modified_at?new Date(document.modified_at).toLocaleString():'Unknown'],['Indexing',document.indexing_status],['Repository path',document.relative_path]].map(([label,value])=><div key={String(label)}><dt className="text-xs text-slate-500">{label}</dt><dd className="mt-1 break-words font-medium">{String(value)}</dd></div>)}</dl>:<p className="rounded-lg border border-dashed border-slate-200 p-4 text-sm text-slate-500">No authorized document relationships are available.</p>}</div>{tab==='ai'?<div className="border-t border-slate-200 p-3"><AIComposer value={question} onChange={setQuestion} onSubmit={submit} placeholder="Ask about this document…" disabled={asking} testId="document-ai-composer"/></div>:null}</div>;
}
