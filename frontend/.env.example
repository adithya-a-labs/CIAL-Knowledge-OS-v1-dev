# CIAL Knowledge OS - Environment Variables
# Copy this file to .env and fill in values for your environment

# API Configuration
# Empty means same-origin /api. Vite proxies it to loopback during development;
# Caddy proxies it in optional Laptop LAN Server Mode.
VITE_API_BASE_URL=

# Authentication
# The current development auth flow uses the backend HttpOnly cookie session.
# For local development, the client normalizes localhost/127.0.0.1 API hosts
# to match the browser host so SameSite cookies survive login -> /auth/me.
VITE_AUTH_CLIENT_ID=
VITE_AUTH_TENANT_ID=
VITE_AUTH_REDIRECT_URI=

# AI Integration (future)
VITE_AI_API_ENDPOINT=
VITE_AI_MODEL_VERSION=

# Feature Flags
VITE_ENABLE_REAL_AI=false
VITE_ENABLE_AUTH=true

